[package]
name = "hooch"
version = "0.1.0"
authors = ["Bartek"]
description = "A small async runtime that's been bootlegged for my projects."
license = "Apache-2.0"
repository = "https://github.com/bwintertkb/hooch"
readme = "README.md"
keywords = ["async", "runtime", "executor", "concurrency", "lightweight"]
categories = ["async", "concurrency", "runtime", "io", "non-blocking", "futures"]
edition = "2021"

[dependencies]
